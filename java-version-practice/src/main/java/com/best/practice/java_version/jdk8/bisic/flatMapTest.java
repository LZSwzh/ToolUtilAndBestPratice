package com.best.practice.java_version.jdk8.bisic;

/**
 * 介绍Lombok的flatMap方法
 * 1. 作用：
 *      1.1 用于链式调用，将流中的元素转换为流，然后将流中的元素合并为一个流
 *      1.2 用于调试，将流中的元素转换为流，然后将流中的元素合并为一个流，方便调试
 * 2. 注意事项：
 *      2.1 flatMap方法的参数是一个函数，函数的参数是流中的元素，函数的返回值是流中的元素
 *      2.2 flatMap方法的返回值是一个流
 */
public class flatMapTest {
    public static void main(String[] args) {

    }
}
